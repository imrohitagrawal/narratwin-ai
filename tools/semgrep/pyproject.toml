[project]
name = "narratwin-semgrep-tool"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = [
    "semgrep==1.172.0",
]

[tool.uv]
package = false
override-dependencies = [
    "mcp==1.28.1",
]
