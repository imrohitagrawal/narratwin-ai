# G1 bibliography successor plan — Issue #540

Refs [#540](https://github.com/imrohitagrawal/narratwin-ai/issues/540), parent [#521](https://github.com/imrohitagrawal/narratwin-ai/issues/521), and retained [#533](https://github.com/imrohitagrawal/narratwin-ai/issues/533).
Prepared 2026-09-14 by implementation sub-orchestrator `g1_package`. This is a pre-code planning draft. Exact-byte independent review and root technical approval are pending; implementation is not authorized by this draft.

## User outcome and boundary

NarraTwin should turn approved knowledge into grounded narration and a coherent, usable controlled demo with complete presenter outputs, real browser/backend playback, captions and export. This package removes a demonstrated source-classification error that blocks the G1 candidate's semantic acceptance: four bibliography entries must remain references rather than requirements. It delivers an inspectable successor and executable proof of that limited correction.

This package does not certify G1, adopt private sources, activate V2, replace V1, complete the 859-group semantic review, accept the comparison amendment, select or call providers, generate media, implement product adapters/UI, or satisfy T06–T08. Cut 1 is neither public launch nor production readiness. The enterprise backlog stays outside this package.

## Current truth, requirements and provenance

| ID | Source and observation | Required effect / limit |
|---|---|---|
| SRC-540-01 | Current user delegation; root [kickoff5667192971](https://github.com/imrohitagrawal/narratwin-ai/issues/521#issuecomment-5667192971), child540 | Root may approve bounded technical decisions within existing grant; agent-authored record is not an owner attestation. |
| SRC-540-02 | Accepted main `57c7dcb8302bce6f007f8443965981fe60b12e23`; PR536/539 merged;535/537/538 closed | Documentation/navigation and sequencing work complete; do not restart it. |
| SRC-540-03 | PR534 closed unmerged, retained head `6fac239e226cfaa2bcd3b79d4e9bbe8e96feb10d` | Its unsigned signing packet is retired, never a successor approval source. |
| SRC-540-04 | [Four-entry reproduction5666139978](https://github.com/imrohitagrawal/narratwin-ai/issues/521#issuecomment-5666139978), [72-context supplement5666628401](https://github.com/imrohitagrawal/narratwin-ai/issues/521#issuecomment-5666628401) | Exactly ADR0000 lines36–39 / vector18–21 change N→R; retained source bytes and all other rows remain unchanged. |
| SRC-540-05 | Existing original mapping, its schema, binding and original validator at frozen main; exact identities in profile | Preserve all eight original artifacts and original validator/test. Original archive and `g1-originals` profile retain original mapping identity. |
| SRC-540-06 | [Recovery handoff](../../work-archive/2026-09-14-recovery/HANDOFF.md), [G1 evidence](evidence/INDEX.json) | Original owner plan and ten masters recovered. No availability gap demonstrated; no private read/copy or resend request. Backup UNPROVED. |
| SRC-540-07 | Existing work procedure/registry and `scripts/work_records.py`233–247 | Issue coverage must match pinned snapshot; preserve prior60 entries and add540 through a new lineage descriptor, not original-source mutation. |
| SRC-540-08 | AGENTS, RCA/playbook, QUALITY_GATES and existing PR template | Adapted source/failure plan before implementation, actual independent review, RED/GREEN, full local/hosted gates, truthful noncertifying evidence and root-only protected merge. |

Original533 carrier body forbids new parser/mapping rebuild. Its authority is historical for the retired packet. Child540 is the explicit additive successor amendment under the current user delegation and root technical decision; it does not pretend the original533 body already granted implementation. Mandatory repository reading was completed before this checkout or file creation, including the full required thirteen sources, docs README/work INDEX/PROCESS, PRD, G1 records and recovery evidence. No private original was read.

## Intended versus implemented and alternatives

| Finding | Reproduction | Disposition |
|---|---|---|
| Bibliography promoted to obligations | Four source-bound IDs are CURRENT_NORMATIVE; frozen ADR heading and context identify references. Both independent lenses reproduced it. | REQUIRED_CONTRACT; correct only the four N→R classifications and derived outputs. |
| Other same-text occurrences might also be references | Independent public context audit covers72 members:4 ADR,62 scoped STAGE_ISSUE_PLAN list obligations,6 external bodies. All six frozen public bodies re-fetched with matching hashes/bytes. | No fifth correction demonstrated. Issue393 line18 remains historical SUPERSEDED_NORMATIVE/PENDING test-mutation context. |
| Original receipt could falsely certify successor | Original exhaustive receipt pins31398/18016 and an earlier preintegration validator subject. | REQUIRED_CONTRACT; retain historical values and introduce explicit successor integration lineage with PENDING certification. |
| Legacy process scope excludes successor files | Clean-main `check_phase1_closure_docs.py` exact allowlist and existing runner routes do not admit this package. Dirty shared `prospective_scope.py` is not merged. | REQUIRED_CONTRACT; extend existing runner narrowly from reviewed manifest, never import the dirty file or weaken preserved checks. |
| Registry addition alone fails | Independent in-memory540 addition returns INVENTORY_COVERAGE against original60-issue source. | REQUIRED_CONTRACT; new ISSUE_SNAPSHOT preserves previous list plus540, source timestamp and completeness meaning. |
| Independent backup absent | Recovery is local exact custody, not independent backup. | ADVISORY_DEBT for this correction; owner/store issue532 retains backup work, UNPROVED. |

Rejected alternatives: mutating the predecessor breaks original custody and archive verification; an overlay-only successor invents a reconstruction protocol downstream could omit; broad profile threading through the roughly4,000-line original validator changes unrelated generation/cache policy; a new network comment authenticator is unnecessary for local structural integrity and would not replace hosted approval. The smallest justified solution is a materialized successor, a narrow new validator, existing production caller registration and existing manifest-based scope enforcement.

## Exact correction and deterministic derivation

The fixed profile owns the complete source tuple, each atom/span/hash and the exact four IDs:

- `MPV2-6FD1AF1B119751D12FE6`, ADR line36, vector18.
- `MPV2-29F6F8A4F175178310E3`, ADR line37, vector19.
- `MPV2-7A08CF034D6B2A3D447D`, ADR line38, vector20.
- `MPV2-D5086F6004054DDDA5F6`, ADR line39, vector21.

Frozen source commit is `b6b0c05c7227428ff0841361f3970b0b2c40aa86`. Preserve its1250 bytes,22 atoms, source and atom IDs. Keep all17 retained ADR normative rows and every other68 same-text occurrence. First invoke the original validator without modification; decode its exact mapping using existing helpers; derive only these four N→R decisions and remove only their four normative rows. Recompute affected decision/ordered/partition hashes, ADR semantic coverage, complete retained row ordering, compact value-table encoding/derived comparisons, ordered requirement digest and duplicate census. Compare the entire candidate to the deterministic expected object, including unchanged fields, rather than accepting internally consistent counts alone.

Conditional expected totals are31394 rows:18012 repository/owner (17309 repository plus703 owner),13382 external. Normative13374→13370 and reference53→57; duplicate groups859 remain, occurrences3103→3099, excess2244→2240. Profile records these as expected values; only actual derivation and independent recomputation can establish the result.

Derive the successor schema from the exact predecessor schema. Change only repository-semantic `partitionSha256/const` and `classCounts/const` NORMATIVE_REQUIREMENT/REFERENCE. Keep original external exhaustive receipt constants `expectedMappingRowCount=31398` and `repositoryMappingRowCount=18016`, original context partition, and original receipt bytes. A schema copy is generated, not a separately hand-maintained contract. `allOf` cannot relax the predecessor's conflicting const values.

The new integration-lineage descriptor owns successor totals/hashes and identifies the old external receipt solely as historical external semantic input. Preserve its independent-validator `validatedMappingSha256=f4e8dff41ce5532860189904bb10a78f2c85e9f70082f321de88740954745bc6` and `PREINTEGRATION_EVIDENCE_BUNDLE_NOT_FINAL_EXHAUSTIVE_MAPPING`; its prior11 mutations/PASS are not successor validation. Original semantic/security reviews remain historical and fresh successor certification remains PENDING.

## Trust, effective configuration and production consumers

The new validator's fixed reviewed registration binds profile path, exact raw profile SHA256, exact first-preflight blob SHA256, and frozen base. Root must independently approve those exact bootstrap identities before implementation. These are stable reviewed artifact identities, not tunable limits. The profile owns counts, source selectors, treatment and non-secret operation limits; the existing preflight owns path and line budgets. No caller may supply its own expected digest through CLI, environment or candidate fields.

Production runner and guardrails use the registered profile. CLI accepts only the known registered profile ID. Unknown or malformed configuration, bool-for-int, nonfinite/nonpositive limits, path traversal, symlinks, missing Git or substitutions fail closed. Effective configuration and both input identities appear in sanitized lineage/validation output. A typed runtime config resolves the profile default Git timeout or the optional non-secret `NARRATWIN_G1_GIT_TIMEOUT_SECONDS` production environment value once at entry. Both CLI and trusted production callers use that same resolver; internal callers may inject the same validated type. Numeric finite strictly positive runtime-representable overrides reach actual new-validator Git subprocess calls and are bound/reported as effective configuration. Tests prove default and production override consumption plus malformed/nonfinite rejection. Operational timeout changes need no registration edit; source selectors, correction treatment, scope and byte ceilings remain governed profile policy and cannot be overridden. A changed governed profile requires a new reviewed registration. Read-only verification reports its own effective execution config without rewriting generated lineage; generation lineage binds the config actually used for generation. This resolver does not reconfigure the unchanged predecessor validator's legacy internal Git calls, whose existing limits remain explicit inherited behavior. No module-global profile mutation or cross-profile cache state is introduced.

A coherently edited fifth correction plus rehashed mapping/profile/preflight still fails the unchanged registration. Changing registration changes the code head and requires new independent review and actual exact-head host approval. Preflight commit order proves chronology only. Local success means STRUCTURAL/PENDING; it does not authenticate GitHub authorship, source adoption, owner acceptance or G1 certification. Actual eligible exact-head APPROVED review and protected merge supply their separate authority boundary.

The runner adds the narrow registered manifest route, reads generic per-file/aggregate budgets and verifies exact branch/base/first-preflight identity. It preserves legacy branch, required-file, publication, live-binding and all global checks. Both original and successor validate on the registered route and merged main. Guardrails' changed-artifact trigger must include successor files and its verifier/registration so alternate callers cannot omit it. Near branches and unavailable history fail closed.

## Branch, allowed paths and budgets

Base: `57c7dcb8302bce6f007f8443965981fe60b12e23`.
Branch: `phase-1-closure-process-540-g1-bibliography-successor`.
First commit: `8add659eec90a21514d3cea193d5f6ffb5a724e7`, containing only `docs/governance/preflights/issue-540.json`.
First-preflight Git blob: `ed9e227c031f6c6e16f922d678ba23eab1e3008a`.
First-preflight raw SHA256: `87c4dc10beb76875cb51244d6f538a1eb9d67f8d138c2d5de2d078e1e31ba527`.
Profile raw SHA256: `51ac965848046e6b5ad06c029ca10a78ae432a840200820be0f0e89ca7fb8c8c`.

Exactly25 paths,4,000 aggregate additions plus deletions, no deletion credit. The4,212 sum of per-path ceilings is not cumulative permission to exceed4,000. Generated mapping/binding/schema additionally have a combined55MiB ceiling; mapping remains strictly below50MiB, compact physical lines charged as declared. No generated media/binaries/vendor/dependency addition is permitted. New snapshot is at most64KiB. Scope/budget drift stops for root substantiation and bounded amendment before growth.

| Allowed path | Charged-line cap | Necessity |
|---|---:|---|
| `docs/governance/preflights/issue-540.json` | 200 | Prospective issue, scope and line budget; first commit only. |
| `docs/work/g1-certification/PLAN.md` | 260 | Canonical bounded execution plan and pre-code source/failure matrix. |
| `docs/work/g1-certification/README.md` | 100 | Keep existing registered plan pointer and link this plan. |
| `docs/work/g1-certification/DECISIONS.md` | 100 | Current decisions and preserved predecessor history. |
| `docs/work/g1-certification/HANDOFF.md` | 150 | Completed/unaccepted/pending state and exact resume ownership. |
| `docs/work/g1-certification/evidence/INDEX.json` | 150 | Sanitized evidence identities and validation limitations. |
| `docs/work/g1-certification/evidence/REVIEWS.md` | 220 | One consolidated independent source and boundary review record. |
| `docs/work/registry.json` | 100 | Register540 and select reviewed issue snapshot; preserve prior ownership. |
| `docs/work/INDEX.md` | 60 | Regenerate navigation from registry, no manual status duplication. |
| `docs/governance/successors/g1-adr0000/superset-mapping-v2.json` | 20 | Materialized deterministic four-entry successor; separately byte-bounded. |
| `docs/governance/successors/g1-adr0000/narratwin-master-program-v2.json` | 1 | Bind new mapping/schema/lineage while keeping authority NONE. |
| `docs/governance/successors/g1-adr0000/superset-mapping-v2.schema.json` | 1 | Derived exact schema changes; preserve old receipt counts. |
| `docs/governance/successors/g1-adr0000/integration-lineage.json` | 100 | Separate successor integration from historical external semantic receipt. |
| `docs/governance/successors/g1-adr0000/profile.json` | 180 | One typed policy/source/delta boundary; no future generated hashes. |
| `scripts/quality/issue521_successor.py` | 650 | Narrow deterministic successor validator and fixed reviewed registration. |
| `tests/unit/test_issue521_successor.py` | 750 | Test-owned independent expected IDs/counts and hostile mutations. |
| `scripts/quality/phase1_closure/runner.py` | 180 | Existing route admission reads manifest budgets; preserve all global checks. |
| `tests/unit/phase1_closure/test_runner.py` | 200 | Exercise exact scope, limits, unavailable history and invocation. |
| `scripts/guardrails_check.py` | 120 | Cover successor paths at alternate production validation caller. |
| `tests/unit/test_guardrails_check.py` | 180 | Prove successor changes cannot skip validation. |
| `docs/STATUS.md` | 100 | Current queue and limited result in substantive PR. |
| `docs/ADR/0080-master-program-v2-prototype-first-governance.md` | 100 | Dated successor-lineage explanation; no original proposal rewrite. |
| `docs/QUALITY_GATES.md` | 60 | Document exact successor commands and proof limits. |
| `docs/work/governance-backlog/HANDOFF.md` | 80 | Refresh STATUS-owned handoff digest after STATUS changes. |
| `docs/work/g1-certification/evidence/ISSUE_SNAPSHOT.json` | 150 | Prior60-entry snapshot plus explicit540 addition, preserving original source. |

## Resource ownership and preserved expenditure

The sole cumulative resource ledger and work queue remain root-owned in the [540 precreation record5667304349](https://github.com/imrohitagrawal/narratwin-ai/issues/540#issuecomment-5667304349), [verified allocation5667510378](https://github.com/imrohitagrawal/narratwin-ai/issues/540#issuecomment-5667510378), root kickoff and STATUS. This plan links them; it creates no second resource/status system. One writer owns this checkout and all listed paths; root/reviewers read only until ownership is explicitly transferred.

Bootstrap allocation is one128MiB checkout plus1MiB planning text, branch and eventual draft PR. Root's native checkout observation is device16777232/inode337233110. Retain branch/worktree until accepted merge and exact evidence transfer; only root may remove finished task-owned resources using enumerated identities. Shared Git object storage,12 prior worktrees (including two dirty), retired533 evidence and private recovery are preexisting and not cleanup targets. Planned mapping retention is separately byte-bounded; environments, scanner databases, build caches, processes, containers and scratch evidence require root's finite precreation ledger allocation before use. Reuse approved535 Python read-only where compatible, but that cannot substitute for later exact hosted uv bootstrap.

Prior5218494/8500 charged lines remain; retired533381/800 cumulative (375 final diff),3/4 review rounds and corrective approach1/2 remain. Earlier G1 approach/review totals are UNKNOWN, not zero. The retained533 ledger has35 resources:8 retained,27 verified absent. Historical semantic review stays20 groups with19 boundedPASS/1FAIL and839 unreviewed. The new540 envelope is additive: one implementation approach plus one bounded RCA correction, maximum four review rounds. The completed paired baseline/in-principle review is round1; the separately activated exact-byte planning review is round2/4. Refining the same pending exact-byte draft remains round2. Round3 is reserved for implementation review and round4 for independent correction verification; no count is reset.

Defaults are zero private reads/uploads/provider creates/USD/retries and no fallback. No account, credential, subscription, clone, outreach, enrollment, deployment or publication operation is allocated. Native scanner caches need their actual explicit TRIVY_CACHE_DIR/GRYPE_DB_CACHE_DIR confinement if later allocated; XDG alone did not establish confinement in historical work. Preserve unknown prior expenditure and failed attempts.

## Invariant, failure and validation matrix

| ID | Invariant / realistic false pass | Discriminating proof and expected result |
|---|---|---|
| F540-01 | Bibliography stays reference while real obligations remain | Test-owned literal IDs/coordinates/counts; original is RED for corrected expectation; successor GREEN removes exactly four rows and keeps17 ADR normative rows and other68 same-text rows. |
| F540-02 | Original source/custody and unrelated semantics immutable | Exact source bytes/atom IDs, all eight artifacts, context partition, external manifest/decisions/restricted receipt hashes and retained decoded row byte equality. Original archive/profile pass; private NOT_CHECKED. |
| F540-03 | Derived counts are not self-reported success | Independent partition/coverage/ordered row-set and compact roundtrip/census computation proves31394/18012/13382/859/3099/2240. |
| F540-04 | Candidate cannot authorize a fifth change | Mutate fifth class, wrong coordinate, extra removal or retained row, profile/preflight/source replacement and stale binding; recompute internal hashes; unchanged independent registration rejects. |
| F540-05 | Historical receipt never becomes current proof | Omit/replay receipt as successor certification, alter successor totals without lineage, claim PASS/activation or swap coherent packet; reject. Original counts/earlier11-mutation status remain historical. |
| F540-06 | No private-public or authority transfer | Original archive/profile unchanged, public NOT_CHECKED/backup UNPROVED; forced-tracked private artifact regression remains active; certification/activation claims rejected. |
| F540-07 | No cache or missing-source false pass | Original→successor→original in one process with mutated config/source and unavailable frozen Git; no cross-profile cached acceptance. |
| F540-08 | Typed configuration reaches actual boundary | Defaults/valid injected overrides, malformed/unknown fields, bool-for-int, nonfinite/range, traversal/symlink and missing inputs fail closed; effective config bound into output. |
| F540-09 | Exact route and alternate callers enforce successor | Actual synthetic Git histories/near branches, missing/changed preflight first blob, wrong base/head, per-path/aggregate/bytes and unavailable history; runner plus guardrails both reject skip/substitution. |
| HUMAN-540-01 | Structural review is not source adoption, product or human acceptance | Root retains exact source/semantic/security/certification and accepted-current gates under521/533;540 cannot close them. |
| HUMAN-540-02 | Merge is authorized and host-enforced | Root observes unchanged reviewed head, real required checks/reviews/conversations/protection and reference-only merge wording immediately before protected merge. |

Focused commands after implementation: `uv run pytest -q tests/unit/test_issue521_successor.py tests/unit/phase1_closure/test_runner.py tests/unit/test_guardrails_check.py`; original521 unit regressions and direct original/successor CLIs; `make work-records-quality`, `make documentation-quality`, `make quality`, `NARRATWIN_POLICY_ONLY=1 make quality`, `uv run ruff check scripts tests`, `uv run mypy scripts tests`, `git diff --check`. Run full backend unit/API/integration plus `make ci`, security/dependency/container/secrets/eval targets and forced PR-event guardrails against the actual final PR body. Synthetic tests and stdlib navigation results do not establish product acceptance.

Before freezing final generated evidence, enumerate and run the actual hosted commands and topology: pinned Python3.13/uv0.11.18 frozen bootstrap, full history, direct-head and PR merge checkouts, push/PR environments and mock-provider values, least-privilege permissions, environment propagation, complete suite/manifests and every required context. Native AMD64 and ARM64 security lanes remain mandatory. Root observed12 required contexts, strict up-to-date checks, one eligible approval after last push, stale-approval dismissal and administrator enforcement. Official GitHub [protected branch](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches) and [rule management](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/managing-a-branch-protection-rule) documentation checked by root2026-09-14 supports this host boundary. Local approximations or agent comments cannot satisfy an APPROVED review. Hosted run links and exact checkout heads must be in final evidence.

## Bootstrap observations, not acceptance

Before the first commit, `UV_NO_SYNC=1 UV_OFFLINE=1 PYTHONDONTWRITEBYTECODE=1 make quality` exited0:29 archive,19 work-record and29 documentation stdlib tests passed; Phase1 governance passed. This checks the baseline plus a not-yet-committed preflight, not complete540 scope. Explicit core preflight evaluation of the planned25 paths returned no findings; evaluating only the actual one-file bootstrap returned expected `GPF.SCOPE.REQUIRED_NOT_CHANGED`. One initial core API invocation used an incorrect positional argument and raised TypeError; correcting the keyword invocation produced these results. No failure was relabeled as pass.

The stdlib tests used their existing temporary-directory fixtures and normal teardown. Their success does not prove an independent before/after machine-wide temporary-resource inventory; root must retain that observation limitation in its cumulative ledger. No dependency environment, generated successor mapping, scanner, Docker object, browser, private source or provider operation was allocated by bootstrap. Exact tree/resource measurements accompany the root handoff; test count is evidence scope, not review quality.

## Independent review prompts and convergence

Source reviewer: verify every four-tuple against frozen public ADR bytes; challenge retention of17 obligations/68 neighboring contexts; verify original/source/atom/context/external receipt custody, exact generated schema delta and receipt chronology; demonstrate any fifth correction rather than infer from matching text. Boundary reviewer: attempt self-consistent fifth-edit/profile/preflight swaps, missing Git/cache contamination and alternate caller bypass; challenge actual scope admission, typed policy and hosted topology/validation sufficiency. Both reviewers review independently of authorship and bind exact bytes/head.

Root substantiates findings and classifies CRITICAL_BLOCKER, REQUIRED_CONTRACT, ADVISORY_DEBT, DUPLICATE or OUT_OF_SCOPE. Only reproduced blockers/contracts block. Consolidate symptoms under root cause, fix the smallest boundary, add meaningful regression RED/GREEN and independently verify correction. Preserve failed candidates, expenditure and review counts. Repeated failures trigger bounded RCA/replan; no automatic retries, budget resets, architecture expansion or advisory-driven blocking.

## Skill and tool selection

Repository docs/templates/validators are the primary workflow. Exposed `work-package-protocol` guidance was consulted in-session for bounded package, precreation resources and preserved counters; the existing StackClimb lock is scoped to524, so this consultation does not claim repository-approved activation. No custom skill/plugin or dependency was installed. Browser/UI/media/provider skills are deferred because540 crosses only public source/integrity/governance boundaries. Public Git/GitHub and stdlib are sufficient for bootstrap; later existing pytest/jsonschema and hosted tooling require their actual boundary proof.

## Completion, escalation and handoff

After both exact-byte planning reviews and root approval, publish the linked draft PR before code, commit meaningful behavioral RED then narrow GREEN, execute the applicable local/full/hosted gates, obtain independent implementation/correction review, and return a recommendation packet. It must contain issue/PR/base/head/scope, practical change, commands/outcomes/run links, findings/corrections/independent verification, limitations/human-only evidence, STATUS/handoffs, exact merge prerequisites and resource/issue disposition. Sub-orchestrator never merges.

Root independently verifies the reviewed head is unchanged, hosted/local required checks and real GitHub requirements hold, conversations are resolved, and reference-only merge wording complies. Root performs normal protected merge, verifies content/post-merge workflows, synchronizes clean main and closes only540 when satisfied.521/533 remain open for outstanding G1 acceptance; record routine merge/check/cleanup facts in issue/PR comments, no successor status-only PR. Finalize STATUS, G1 evidence and both affected handoffs in this substantive PR.

Escalate before any unlisted path, budget ceiling, extra correction/review round, changed source treatment, actual private availability gap, identity mismatch, spending/credential/account/privacy/rights decision, destructive cleanup, or nondelegable host approval. Root may make routine technical amendments within the current user grant; elapsed time never supplies missing approval. Pending external authority does not excuse unfinished independent authorized work.

Completed/accepted:536/539 navigation/sequencing and T05 audio; original recovery identity remains retained. Implemented but unaccepted: original G1 candidate and this bootstrap; no successor implementation yet. Pending: exact-byte plan review/root approval, correction implementation/validation, remaining semantic/security certification and source adoption, distinct accepted-current transition, G2/G3 as applicable, comparison amendment, quality-first519/512 experiments and520 actual UI/capture, T06 complete presenter outputs, T07 real integration and T08 exact-artifact acceptance. Next authorized action is review of these bootstrap bytes; substantive work waits for root's recorded technical approval and finite resource allocation.
