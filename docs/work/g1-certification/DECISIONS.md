# G1 source certification: decisions

## 14 September 2026 — stable ownership

Source: the current user instruction after the reviewed structure, “Please go ahead and implement it.” Root implements that design under delegated authority. This record organizes existing work; approval and delivery state remain in their authoritative documents.

Rationale: stable IDs and shared references prevent missing sources and competing copies. Completed work keeps its path. Preserve [earlier recovery decisions](../../work-archive/2026-09-14-recovery/DECISIONS.md) and [review limitations](../../work-archive/2026-09-14-recovery/REVIEW.md). Record new decisions with source, owner, reason, effect, supersession and next action. No acceptance or spending authority follows from registration.

## 14 September 2026 — delegated sequencing disposition

Source: the user instructed the main orchestrator to act as an expert and “take decision on my behald.” Root chose independent documentation delivery after two reviewers verified the concrete [delegated sequencing decision](https://github.com/imrohitagrawal/narratwin-ai/issues/535#issuecomment-5666016246); a third independently reproduced the G1 bibliography defect. This is delegated agency, not a claim that the user personally certified source normalization.

PR534 is retired unmerged. Preserve head6fac239e, base71e0568, its frozen artifacts and unsigned packet as history; do not sign or consume the old example. PR536 merged normally at67d4b094 with approved29606 tree parity and passing main checks. The prior carrier-first ordering is superseded for this documentation delivery; certification, accepted-current and provider requirements remain.

[Issue #538 / PR #539](https://github.com/imrohitagrawal/narratwin-ai/pull/539) reconciles current active records under Issue535. Its first preflight-only commit2d7269c5 and linked draft PR precede edits; the necessary STATUS-handoff refinementc547de52 precedes its binding update;17 paths/2000 additional charged lines preserve earlier99 paths/9066 lines and the35398 program ceiling. After this PR, root prepares a separately bounded G1 correction and fresh lineage from the [public correction analysis](https://github.com/imrohitagrawal/narratwin-ai/issues/521#issuecomment-5666139978). Historical sources/reviews are retained, no acceptance is fabricated, and no further design decision is pending from the user.

## 14 September 2026 — bounded four-bibliography successor

Current user delegation and root [exact-byte technical approval5667612108](https://github.com/imrohitagrawal/narratwin-ai/issues/540#issuecomment-5667612108) authorize Issue540, reviewed plan and profile, with25 exact paths/4000 additional charged lines. This is a new additive correction package under521/533; the old533 no-parser carrier does not itself authorize these changes.

Decision: preserve all eight predecessor artifacts and use an independent fixed registration plus deterministic four-entry derivation. Schema changes are limited to the new partition hash and two class-count values; original external receipt counts remain historical. A separate lineage descriptor is PENDING and cannot transfer predecessor approval, legacy11-mutation evidence or retired534 receipts. Operational Git timeout has a typed production override; governed input identity and limits do not.

Implementation lead owns the isolated540 checkout; root owns cumulative resources, queue, finding dispositions, technical approval and merge. Independent authorship-separated review is recorded in REVIEWS.md. Prior5218494/8500, retired533381/800 cumulative and3/4 rounds/1of2 corrections, unknown earlier totals,20-group history and839 unreviewed remain. Two540 planning review rounds are consumed; implementation round3 and correction verification round4 remain bounded.

After structural delivery, root selects the next eligible G1 certification/source-adoption work under the existing acceptance contract; no accepted-current transition, G2 activation or Cut1 acceptance is implied.

Root navigation refinement after implementation: registry.plan now binds the exact approved PLAN.md directly; README is navigation. This supersedes the earlier PLAN table's pointer-retention choice without changing that reviewed historical document or its hash. Existing work-record consumer validation and handoff hash enforce the direct plan identity.

## 14 September 2026 — bounded correction approach2 after round3

Root [correction grant5668117961](https://github.com/imrohitagrawal/narratwin-ai/issues/540#issuecomment-5668117961) substantiates two REQUIRED_CONTRACT findings on preserved45c9b7a. F540-09: availability of the registered first-preflight Git object does not prove it is in the candidate's history; generic preflight validation reads final bytes and can miss an alternate first packet later restored. F540-10: the new binding records mapping/schema identity but omits the promised lineage path/hash/bytes. These are separate responsible boundaries; source row correction and unchanged predecessor evidence independently pass.

Smallest correction: common history validation verifies the registered preflight is an ancestor of actual HEAD; the bounded issue route additionally requires that registered commit to be the first commit on its first-parent range from the frozen base. Main and actual merge-checkout consumers retain common ancestry validation, preserving normal merge-commit history. Real Git tests keep the GPF adapter present at base, preserve an unrelated registered object, restore final preflight bytes after a wrong first packet, and test a late merge of the registered object; these must reject without relying on mocked history.

Render integration lineage before the successor binding, then bind its path, exact raw SHA256 and byte count. Missing/stale linkage must reject; a valid generation-timeout override must update lineage and its binding together. Regenerate only the derived binding/lineage as required; mapping must remain SHA256acc8a3ada7db50ba8910d4f7a53dd166a431e48dd754085d280bd30c9b141cfc and original artifacts remain unchanged.

Record RED before these implementation corrections, then targeted GREEN, full corrected-head Python/integration checks and independent round4 verification. This consumes the previously reserved bounded RCA correction approach2, not a reset or new scope. All25 paths/4000 lines, original bootstrap hashes, failed attempts, prior expenditure and four-round review envelope remain. No further defect class or budget growth is silently absorbed.

Hosted merge-checkout refinement within F540-09: common validation binds the registered preflight to the actual checkout's ancestry. The issue scope checks the candidate's first-parent sequence; in a hosted pull-request merge checkout, its candidate selector must equal the exact second parent of the actual two-parent merge (or actual HEAD for a direct-head checkout). An unrelated environment selector fails closed. This preserves direct-head/push, genuine PR merge and eventual normal main merge behavior without accepting a late-merged alternate preflight history.

Before round4, root independently reproduced an additional F540-08 runtime-boundary violation: threading.TIMEOUT_MAX passes the typed configuration but actual Python3.13 subprocess polling raises OverflowError. The thread wait limit is not the process polling limit. Within the same approach2 and unchanged scope, replace that mistaken invariant with a conservative portable signed32-millisecond polling limit rounded down to whole seconds. Prove real Git completes at the literal2147483-second boundary and reject2147484 and the legacy threading maximum at configuration entry. Keep ordinary timeout configuration, reviewed default, profile/preflight and artifact identities unchanged. Catching an overflow after operation entry is insufficient. This root finding is additive to the two round3 findings and does not reset counts or authorize a new architecture.

Root approved the exact runtime correction in [5668375986](https://github.com/imrohitagrawal/narratwin-ai/issues/540#issuecomment-5668375986) after actual Git probes and CPython3.13.7 selectmodule.c's signed INT_MAX millisecond boundary:2147483 and2147483.647 succeed,2147484 and threading.TIMEOUT_MAX overflow. The governed implementation conservatively accepts whole-second maximum2147483, with ordinary fractional overrides below that ceiling unchanged.

## 14 September 2026 — bounded fixture correction approach3

Root [5668949691](https://github.com/imrohitagrawal/narratwin-ai/issues/540#issuecomment-5668949691) approves exact [plan5668908607](https://github.com/imrohitagrawal/narratwin-ai/issues/540#issuecomment-5668908607),5597bytes/SHAbc9d74110e8a33007201f94dab9cca3040013d3dfbbe9a4a1c2a6e8d41586f8c, after both independent round5 plan PASS. This prospectively adds approach3 and narrow round6, retaining consumed approaches1–2/rounds1–4 and all failures/expenditure. Immutable PLAN/profile/preflight and25-path4000-line scope remain.

F540-13 is a REQUIRED_CONTRACT fixture-construction defect: copying ROOT/.git assumes the linked-worktree file seen locally; Actions uses a directory and both backend jobs fail26 setup cases. Earlier actual-history tests missed this source-checkout topology. Extract the old behavior for real standalone/linked distinct-HEAD RED, then resolve --absolute-git-dir through existing typed successor.git and write an absolute pointer. Never use common-dir for HEAD identity. All tiny-repository writes/registration remain inside owned fixtures with isolated Git config/dummy identity/signing disabled; borrowed metadata stays read-only.

Only the test helper/regressions and these existing records change. Preserve all eight predecessors/four outputs/three bootstrap identities; verify HEAD, historical blob and ancestry for both forms, focused caller tests, make quality, full local make ci and actual corrected-head hosted push/PR/merge suites. Root separately owns hosted exposure and merge eligibility. Same10800-second cumulative and7GiB scratch limits retain2172.764seconds consumed; no cleanup or provider/source-adoption authority follows.
